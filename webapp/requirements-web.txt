Django==6.1.1
djangorestframework==3.18.1
django-cors-headers==4.9.0
pytest==9.1.1
pytest-django==4.14.0
celery==5.6.3
redis==8.1.0

# Only needed in prod (DB_ENGINE=postgres, cmweb.settings.prod) - dev/test
# use SQLite (see cmweb/settings/base.py).
# psycopg[binary]
